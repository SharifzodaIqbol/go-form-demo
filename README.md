# Простой HTTP-сервер на Go для обработки формы
* Отображает HTML-форму (user.html).

* Обрабатывает POST-запросы с данными формы (имя и возраст).

* Возвращает ответ с введёнными пользователем данными.
